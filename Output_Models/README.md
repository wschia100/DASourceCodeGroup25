# Output_Models

Store trained model outputs here (for example: `.pkl`, `.joblib`, `.onnx`, checkpoints).

Model artifacts in this directory are ignored by Git.
This README is tracked to keep the folder in the repository.
